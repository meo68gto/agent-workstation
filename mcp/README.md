# MCP Servers for Grok Bot

Grok Bot is MCP-native. This directory holds example MCP server configurations that a Bot can load.

## Example

See `examples/servers.example.json` for a minimal starting point.

When running inside Grok Bot, prefer official connectors and MCP servers over browser automation whenever possible (see AGENTS.md connector-first hierarchy).
